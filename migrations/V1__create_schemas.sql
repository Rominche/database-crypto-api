-- V1: Create base schemas for database-crypto-api
-- sensitive: schema for sensitive / personal data
-- analytics: schema for aggregated / analytical data

CREATE SCHEMA IF NOT EXISTS sensitive;

CREATE SCHEMA IF NOT EXISTS analytics;
